# 📞 Система управления АТС (Database Edition) | Лабораторная работа №5

Десктопное приложение для управления Автоматической Телефонной Станцией (АТС).
Проект является развитием Лабораторной работы №4 с переходом от хранения данных в оперативной памяти к использованию реляционной базы данных **SQLite**.

## 🎯 Цель работы
Реализация десктопного приложения с графическим интерфейсом (GUI) на C++/Qt, поддерживающего постоянное хранение данных в БД, CRUD-операции и расширенную работу с файловой системой (Бэкап/Восстановление и Импорт/Экспорт).

## 🚀 Основные возможности

### 1. Работа с Базой Данных (SQLite)
* **Персистентность:** Все данные (тарифы, клиенты, звонки) автоматически сохраняются в файл `atc_database.sqlite`.
* **Целостность данных:** Реализована проверка ссылочной целостности (нельзя добавить звонок для несуществующего клиента).
* **Автоматическая инициализация:** При первом запуске приложение само создает необходимые таблицы SQL.

### 2. Управление файлами (Два режима)
Приложение поддерживает два типа операций с файлами через панель инструментов:
* **Резервное копирование (Backup/Restore):**
    * 💾 **Бэкап БД:** Создает полную копию файла `.sqlite`.
    * 📂 **Восстановление:** Заменяет текущую базу данных из файла резервной копии.
* **Обмен данными (Import/Export):**
    * 📄 **Экспорт в CSV:** Выгружает содержимое таблиц в текстовый формат (совместим с Excel).
    * 📥 **Импорт из CSV:** Загружает данные из текста в базу данных.

### 3. Функционал АТС (CRUD)
* **Тарифы:** Управление стоимостью звонков и платой за соединение.
* **Клиенты:** Учет абонентов и их баланса.
* **VIP-клиенты:** Расширенный учет с использованием **множественного наследования** (скидки, персональные менеджеры).
* **Звонки:** Регистрация звонков с автоматическим расчетом стоимости.
* **Статистика:** Динамический подсчет общей выручки и активности абонентов.

## 🛠 Технический стек

* **Язык:** C++17
* **Фреймворк:** Qt 6 (Widgets, SQL)
* **СУБД:** SQLite (драйвер `QSQLITE`)
* **Среда:** Qt Creator / QMake

## 📂 Структура проекта

| Файл | Описание |
|------|----------|
| `main.cpp` | Точка входа в приложение. |
| `mainwindow.h/cpp` | Главное окно, UI, слоты для кнопок и таблиц. |
| `DataManager.h/cpp` | **Ключевой класс.** Отвечает за подключение к БД, SQL-запросы и логику бэкапов. |
| `atc_database.sqlite` | Файл базы данных (создается автоматически). |
| `Person.h`, `Client.h` | Базовые классы (Виртуальное наследование). |
| `VIPClient.h/cpp` | Класс с **множественным наследованием**. |
| `Tariff.h`, `Call.h` | Классы данных с перегрузкой операторов. |

## ⚙️ Установка и Запуск

### Предварительные требования
* Установлен **Qt Creator** и библиотека **Qt 6** (с компонентом `Qt SQL`).

### Инструкция
1.  Откройте файл проекта `atc_gui.pro` в Qt Creator.
2.  **Важно:** В файле `DataManager.cpp` (метод `connectToDatabase`) проверьте путь к базе данных. Сейчас он настроен жестко:
    ```cpp
    QString dbPath = "/Users/kostiashka/lab4_atc_gui/atc_database.sqlite";
    ```
    *(При переносе на другой компьютер измените этот путь на актуальный).*
3.  Запустите проект (Ctrl+R / Cmd+R).
4.  Приложение автоматически создаст файл БД и таблицы.

## 🔍 Сценарий тестирования (для защиты)

1.  **Запуск:** Откройте приложение. Убедитесь, что статистика пуста (или подгрузилась из прошлого сеанса).
2.  **Добавление:** Добавьте Тариф и Клиента. Перезапустите приложение. Данные должны сохраниться (проверка работы SQLite).
3.  **Звонок:** Попробуйте добавить звонок. В списке абонентов будут только созданные клиенты (защита целостности).
4.  **Бэкап:** Нажмите "💾 Резервная копия БД", сохраните файл на рабочий стол.
5.  **Очистка:** Нажмите "🗑️ Очистить БД". Все таблицы станут пустыми.
6.  **Восстановление:** Нажмите "📂 Восстановить БД", выберите файл с рабочего стола. Данные вернутся.
7.  **CSV:** Попробуйте сделать Экспорт в CSV и открыть файл в Excel (кириллица отобразится корректно).

## 👤 Автор
Студент группы 5130902/40004
**Рыбалкин К.М.**
